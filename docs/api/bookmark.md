## 书签接口

书签用于记录歌曲的播放位置，方便用户从上次位置继续播放。

---

### 获取书签列表

`POST /api/bookmark/list`

获取当前用户的所有书签（由于用户可能有不同设备，所以可能存在多个书签）


### 创建/更新书签

`POST /api/bookmark/create`

创建书签。若该用户已对该歌曲创建过书签，则更新位置（同一用户在同一个设备上只能有一个书签）


### 删除书签

`POST /api/bookmark/delete`
